问题：缓冲纹理没有正确绘制到屏幕，全白
原因：图片路径写错了
后果：憋了我很久
解决方案：把路径改了不就完了吗。

============================================
问题：缓冲纹理没有正确绘制到屏幕，只绘制出一个完整的marble.jpg
原因：
    glBindTexture(GL_TEXTURE_2D, texture_color_buffer);
    第一个参数用错，错用成 GL_TEXTURE
解决方案：加上 _2D

============================================

这个API应该记一下
glViewport(0, 0, w, h);
